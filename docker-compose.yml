version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: alert_service_db
    environment:
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
      POSTGRES_DB: test_db
    ports:
      - "5432:5432"